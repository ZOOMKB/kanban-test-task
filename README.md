# Kanban-test-task
Test web application

## Requirements

- Node.js (I am using version 16.14.0)
- git

## Installation

1. In the terminal, run the command "npm i"
2. To start the project, run the command "npm start" in the terminal